var express = require('express');
var router = express.Router();

// var data = require('./../database.json'); // TODO: update the database.json file

// /* GET users listing. */
// router.get('/', function(req, res, next) {
//   res.json(data);
//   });

module.exports = router;